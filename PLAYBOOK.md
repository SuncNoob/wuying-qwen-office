# 千问办公（AgentScope）

三台无影 Agentic Computer 只通过本仓库协作：

- planner 拆 plan → implement + review
- builder 用本机 Codex 写 AgentScope 应用（mode=codex）
- reviewer 验收 README / requirements.txt / app.py，且代码引用 agentscope

密钥用 DASHSCOPE_API_KEY，只放环境变量或 `.env`（不进 Git）。
